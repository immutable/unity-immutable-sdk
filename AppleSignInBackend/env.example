# Server Configuration
PORT=3000

# Apple Configuration
# The Bundle ID configured in Apple Developer Console for Sign in with Apple
APPLE_BUNDLE_ID=com.immutable.Immutable-Sample-GameSDK

# Auth Service Configuration
# Where to send requests (BFF forwards to this URL)
IMX_AUTH_API_URL=https://api.sandbox.immutable.com

# Immutable Credentials (from Hub project)
# This BFF runs server-side, so the secret key is safe here
IMMUTABLE_API_KEY=sk_imapik-test-YOUR-SECRET-KEY-HERE

# Passport Client ID
PASSPORT_CLIENT_ID=YOUR-CLIENT-ID-HERE
